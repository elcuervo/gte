services:
  gte:
    build:
      context: ../../
      dockerfile: bench/rails/Dockerfile
    image: gte-bench:latest
    environment:
      BENCH_RUNTIME: gte
      BENCH_MODEL: ${MODEL:-siglip2}
      MODEL_DIR: /models/${MODEL:-siglip2}
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-2}
      GTE_SESSION_POOL_SIZE: ${GTE_SESSION_POOL_SIZE}
    volumes:
      - ../../models:/models:ro
    ports:
      - "${GTE_PORT:-3001}:3000"
    cpus: 4
    mem_limit: 3g
    restart: "no"

  pure-ruby:
    build:
      context: ../../
      dockerfile: bench/rails/Dockerfile
    image: gte-bench:latest
    environment:
      BENCH_RUNTIME: pure_ruby
      BENCH_MODEL: ${MODEL:-siglip2}
      MODEL_DIR: /models/${MODEL:-siglip2}
      WEB_CONCURRENCY: ${WEB_CONCURRENCY:-2}
    volumes:
      - ../../models:/models:ro
    ports:
      - "${PURE_PORT:-3002}:3000"
    cpus: 4
    mem_limit: 3g
    restart: "no"
